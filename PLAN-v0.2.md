# vitepress-allyouneed v0.2 — 自动视图工程方案

> 设计文档,**不含代码**。等你确认后进入实施阶段。

---

## 0. 设计原则(沿用 v0.1 三条)

1. **零摩擦**:用户照常写笔记,视图自动出现;无需手动建 .md、register 组件、配 sidebar
2. **系统化索引**:全部视图共用 v0.1 的 `VaultIndex`,无重复扫描
3. **可覆盖**(v0.2 新加):用户可以**像覆盖 VitePress 默认主题一样**改我们的组件 / 样式 / sidebar 行为

---

## 1. 用户视角

零配置场景 —— 用户改 **一行**:

```ts
// .vitepress/theme/index.ts
import AllYouNeedTheme from 'vitepress-allyouneed/theme'  // ← v0.1 是 default theme + style.css,v0.2 改成完整 theme
export default AllYouNeedTheme
```

第一次 `vitepress dev` 启动后,自动得到:

- `srcDir/graph.md` / `stats.md` / `tags.md` 自动生成(已存在不覆盖,告警)
- sidebar 末尾自动出现 "Vault Views" 分组,三个条目
- 浏览器访问 `/graph` 看力导向图,`/stats` 看统计仪表盘,`/tags` 看标签云
- 主题色、字体全跟 VitePress(暗色/亮色自动跟随)

想覆盖任何东西时,标准 VitePress theme 三层洋葱:

```ts
// .vitepress/theme/index.ts
import AllYouNeedTheme from 'vitepress-allyouneed/theme'
import MyCustomGraph from './MyCustomGraph.vue'
import './my-overrides.css'   // CSS 后引入 → 后覆盖

export default {
  ...AllYouNeedTheme,
  enhanceApp(ctx) {
    AllYouNeedTheme.enhanceApp?.(ctx)
    ctx.app.component('VaultGraph', MyCustomGraph)  // 整个组件换掉
  },
}
```

或者只改色:

```css
/* my-overrides.css */
:root {
  --ayn-graph-node-color: #ff4081;
  --ayn-stats-card-bg: #fafafa;
}
```

---

## 2. 架构总览

```
vitepress-allyouneed v0.2 包
├── dist/  (v0.1 已有)
│   ├── index.{js,cjs,d.ts}
│   ├── vitepress.{js,cjs,d.ts}
│   ├── vite.{js,cjs,d.ts}
│   └── markdown-it.{js,cjs,d.ts}
├── style.css  (v0.1 已有)
└── theme/                           ← v0.2 新增,完整 theme 入口
    ├── index.ts                     ← 入口,导出 { enhanceApp, Layout, extends: DefaultTheme }
    ├── components/
    │   ├── VaultGraph.vue
    │   ├── VaultStats.vue
    │   └── Tags.vue
    └── styles/                      ← 组件 CSS,全用 CSS variables
        ├── graph.css
        ├── stats.css
        └── tags.css
```

新加 package.json export:

```json
"exports": {
  ".": { ... },
  "./theme": {
    "types": "./dist/theme/index.d.ts",
    "import": "./dist/theme/index.js",
    "require": "./dist/theme/index.cjs"
  }
}
```

---

## 3. 虚拟视图 .md 文件生成

### 3.1 生成时机与路径

- **何时**:Vite 插件 `configResolved` 钩子结束后(确认 srcDir 已知)
- **何处**:`srcDir/{graph,stats,tags}.md`(根目录,URL 干净 `/graph` `/stats` `/tags`)
- **谁负责**:Vite 插件代码

### 3.2 冲突防护

启动时对每个目标文件:

1. 文件**不存在** → 写入我们的模板内容
2. 文件**存在**且包含我们的 sentinel header(`<!-- generated by vitepress-allyouneed -->`)→ 覆盖(允许我们升级模板)
3. 文件**存在**但**没有** sentinel → **不覆盖**,log warn:
   > `vitepress-allyouneed: 检测到你已有 'graph.md',视图自动注入跳过。要启用视图请删除该文件,或在配置里 views.names.graph 改名,或 views.enabled.graph: false 关掉。`

### 3.3 配置选项

```ts
{
  views?: {
    enabled?: { graph?: boolean; stats?: boolean; tags?: boolean }  // 默认全 true
    names?: { graph?: string; stats?: string; tags?: string }       // 默认 'graph' / 'stats' / 'tags'
    sidebar?: 'auto' | false                                         // 默认 'auto',加在末尾;false = 不动 sidebar
    sidebarText?: { graph?: string; stats?: string; tags?: string; group?: string }
                                                                     // 默认 group: 'Vault Views',项: '关系图' / '统计' / '标签'
  }
}
```

### 3.4 生成的 .md 模板示例

```md
<!-- generated by vitepress-allyouneed (do not edit; will be regenerated) -->
---
title: 全站关系图
layout: doc
---

# 全站关系图

<VaultGraph />
```

`stats.md` 和 `tags.md` 同构,改组件名和标题。

### 3.5 dev 时是否监听 / 重生?

- dev:不监听这三个文件(假设我们生成的不变)。若用户手动删了 → 下次 `vitepress dev` 重启时再生
- build:每次 build 开始时检查并生成,build 结束不清理(用户的 git 里可能没有,提供 .gitignore 模板)
- `.gitignore` 模板内容(写到 README 让用户 copy):

  ```
  # vitepress-allyouneed v0.2 自动生成的视图(每次 build 会重生,不入库)
  /graph.md
  /stats.md
  /tags.md
  ```

---

## 4. Vue 组件 ship 机制

### 4.1 theme 入口

```ts
// src/theme/index.ts
import DefaultTheme from 'vitepress/theme'
import type { Theme } from 'vitepress'
import VaultGraph from './components/VaultGraph.vue'
import VaultStats from './components/VaultStats.vue'
import Tags from './components/Tags.vue'
import './styles/graph.css'
import './styles/stats.css'
import './styles/tags.css'

const theme: Theme = {
  extends: DefaultTheme,   // VitePress 1.0+ 标准 theme 继承字段
  enhanceApp({ app }) {
    app.component('VaultGraph', VaultGraph)
    app.component('VaultStats', VaultStats)
    app.component('Tags', Tags)
  },
}

export default theme
```

**关键**:`extends: DefaultTheme` 让用户继承时,DefaultTheme 链路也保留。用户 `import AllYouNeedTheme from 'vitepress-allyouneed/theme'` 拿到的就是已经继承了 DefaultTheme 的对象。

### 4.2 用户的三层覆盖

| 想覆盖什么 | 怎么做 |
|---|---|
| 单个组件全换 | 用户 `app.component('VaultGraph', MyCustom)` |
| 只改色 / 改样式 | 用户 CSS 文件**后** import,改 `--ayn-*` 变量 |
| 改组件**部分行为** | 不直接支持,需用户写新组件包一层(常规 Vue 模式) |

---

## 5. 数据流 — vault-data.json

### 5.1 生成

- Vite 插件 `configResolved`(dev)/ `buildStart`(build)时,把 VaultIndex 关键摘要序列化:

  ```ts
  interface VaultData {
    nodes: Array<{ id: string; title: string; url: string; tags: string[]; mtime: number }>
    edges: Array<{ source: string; target: string; type: 'wikilink' | 'transclusion' }>
    tags: Record<string, { count: number; files: string[] /* by url */ }>
    stats: {
      totalFiles: number
      totalAssets: number
      totalWikilinks: number
      totalTags: number
      mostRecent: Array<{ url: string; title: string; mtime: number }> // 最近修改 10
    }
  }
  ```

- 写到 `srcDir/public/vault-data.json`(VitePress 的 public 目录,自动复制到 dist)
- 文件大小估计:1000 笔记 ~200KB,10000 笔记 ~2MB(gzip 后 1/4)

### 5.2 组件读

```ts
// 三个组件公用一个 composable
import { ref, onMounted } from 'vue'
import { withBase } from 'vitepress'

export function useVaultData() {
  const data = ref<VaultData | null>(null)
  onMounted(async () => {
    data.value = await fetch(withBase('/vault-data.json')).then(r => r.json())
  })
  return data
}
```

`withBase` 确保 base 前缀正确。dev 时 fetch /vault-data.json → middleware/Vite serve;build 后 fetch 真实 static file。

### 5.3 增量更新

dev 模式下 `handleHotUpdate`:文件改动 → 重新生成 vault-data.json → 触发 Vite HMR → 组件 fetch 新数据。

---

## 6. Sidebar 自动注入

在 `defineConfigWithAllYouNeed` wrapper 里:

```ts
const sidebarConfig = config.themeConfig?.sidebar
const viewsGroup = {
  text: pluginOptions.views?.sidebarText?.group ?? 'Vault Views',
  collapsed: true,  // 默认折叠,不干扰
  items: [
    enabledViews.graph && { text: '关系图', link: '/graph' },
    enabledViews.stats && { text: '统计',   link: '/stats' },
    enabledViews.tags  && { text: '标签',   link: '/tags' },
  ].filter(Boolean),
}

if (Array.isArray(sidebarConfig)) {
  sidebarConfig.push(viewsGroup)
} else if (sidebarConfig && typeof sidebarConfig === 'object') {
  // VitePress 的 per-path sidebar:{ '/': [...], '/guide/': [...] }
  // 加到每个 path 的末尾
  for (const path of Object.keys(sidebarConfig)) {
    sidebarConfig[path].push(viewsGroup)
  }
}
// 若 sidebarConfig 是 undefined,新建 sidebar
```

支持 `views.sidebar: false` 跳过该步骤。

---

## 7. 三个组件具体设计

### 7.1 VaultStats(最简单,先写)

- 顶部 4 张卡片:总笔记数 / 总标签数 / 总链接数 / 总 asset 数
- 中部:"最近修改" 10 条,每条点击跳到对应页
- 底部:"扫描告警" 列表(如果 VaultIndex.warnings 不为空)
- ~80 行 Vue + ~30 行 CSS

### 7.2 Tags(中等)

- 标签云:每个标签字号根据 `count` 归一化(min 0.85em, max 1.6em);点击展开/收起
- 展开后:该标签下笔记列表,按 mtime 倒序;每条带链接
- 支持搜索框过滤标签
- ~120 行 Vue + ~40 行 CSS

### 7.3 VaultGraph(最复杂)

- d3-force(`d3-force` + `d3-selection` + `d3-zoom` + `d3-drag`,只 import 用到的子包)
- 数据:`data.nodes` + `data.edges` 直接喂给 forceSimulation
- SVG 渲染(<500 节点流畅),节点 = `<circle>`,边 = `<line>`
- 交互:
  - hover 节点显示 tooltip(title + tags)
  - 点击节点跳页面
  - zoom + pan(d3-zoom)
  - drag 节点(d3-drag)
- 性能保护:节点数 > 配置阈值(默认 500)时禁用 force simulation,只静态渲染并提示"vault 太大,转用其他视图"
- ~200 行 Vue + ~50 行 CSS

---

## 8. 新依赖

| 依赖 | 体积(min+gz)| 用途 |
|---|---|---|
| `d3-force` | ~10KB | 力导向算法 |
| `d3-selection` | ~5KB | DOM 操作 |
| `d3-zoom` | ~6KB | 缩放/平移 |
| `d3-drag` | ~3KB | 节点拖拽 |
| **合计 d3 子包** | **~24KB** | VaultGraph 用 |

Vue 不算新依赖(VitePress 自带)。

---

## 9. 内部目录(v0.2 新增)

```
src/
├── (v0.1 现有)
│   ├── index.ts / markdown-it.ts / vite.ts / vitepress.ts
│   ├── core/
│   ├── modules/
│   └── utils/
└── theme/                          ← 新增
    ├── index.ts                    ← theme 入口
    ├── components/
    │   ├── VaultGraph.vue
    │   ├── VaultStats.vue
    │   └── Tags.vue
    ├── composables/
    │   └── useVaultData.ts         ← 共享 fetch
    ├── styles/
    │   ├── shared.css              ← CSS variables 定义
    │   ├── graph.css
    │   ├── stats.css
    │   └── tags.css
    └── types.ts                    ← VaultData 类型

src/core/views/                     ← 新增,Vite 插件层
├── generate-md.ts                  ← 生成 srcDir/{graph,stats,tags}.md
├── generate-data.ts                ← 生成 public/vault-data.json
└── sidebar-inject.ts               ← sidebar 自动追加逻辑
```

tsup config 加新 entry:

```ts
entry: {
  index: 'src/index.ts',
  'markdown-it': 'src/markdown-it.ts',
  vite: 'src/vite.ts',
  vitepress: 'src/vitepress.ts',
  'theme/index': 'src/theme/index.ts',   // 新增
}
```

Vue 组件 .vue 文件 tsup 默认不处理,需 `tsup-plugin-vue` 或者用 `@vitejs/plugin-vue` + 自定义 build 步骤。也可以**只把 theme/index.ts 编译,.vue 文件按原样 ship**(类似 element-plus 的策略),让用户的 VitePress 编译时处理。

—— 这一步需要技术评估,见 §11 风险。

---

## 10. 实施步骤(确认方案后)

| 阶段 | 内容 | 估时(LOC) |
|---|---|---|
| 1 | theme 入口骨架 + 3 个空 .vue 组件 + style.css 拆分 | ~150 |
| 2 | views/generate-md.ts(虚拟 .md + 冲突防护)+ 选项 schema | ~120 |
| 3 | views/generate-data.ts(JSON 序列化 + Vite emit + HMR)| ~100 |
| 4 | views/sidebar-inject.ts(三种 sidebar 形态适配) | ~60 |
| 5 | useVaultData composable + 3 组件共用基础设施 | ~50 |
| 6 | **VaultStats 组件** | ~100 |
| 7 | **Tags 组件** | ~150 |
| 8 | **VaultGraph 组件**(d3-force) | ~250 |
| 9 | CSS variables 系统 + 暗色模式 | ~80 |
| 10 | 单元 + 端到端测试(虚拟文件生成、数据 JSON、冲突检测、sidebar 追加) | ~200 |
| 11 | 示例 vault 加视图演示;CHANGELOG;v0.2.0 发布 | ~50 |
| **合计** | | **~1310 LOC** |

预计交付周期:**1-2 个迭代**(假设没踩大雷)。

---

## 11. 风险与待确认

| # | 风险 | 缓解 |
|---|---|---|
| 1 | **tsup 处理 .vue 文件** | tsup 不原生支持 .vue 编译。两个选择:(a) 改用 unbuild / @vue/compiler-sfc 手动 build;(b) ship 未编译的 .vue,让用户 VitePress 编译。后者更简单但要 .vue 进 dist。**v0.2 决策**:倾向 (b),已有先例(element-plus、naive-ui) |
| 2 | **srcDir 在外、Vite fs.allow 限制** | v0.1 已经处理,这里同样适用 |
| 3 | **graph.md 文件污染 Obsidian** | Obsidian 会看到这三个 .md 出现在 vault 里。提醒用户加 Obsidian 的"Excluded files" 或 `.obsidian/app.json` 配置 |
| 4 | **大 vault 节点数爆炸** | VaultGraph 加节点数阈值(默认 500),超过转 placeholder |
| 5 | **dev HMR 数据同步** | 改 .md → 重新生成 vault-data.json → Vite HMR;组件用 `<script setup>` + reactive ref 自动 re-render。需测 |
| 6 | **sidebar 用户用 function 而非 object/array** | VitePress 允许 `sidebar: () => [...]`。我们要先 await 这个函数再追加。需测 |

---

## 12. 等你拍板的事

1. **`.vue` 文件 ship 方式**:用 (b) 编译进 dist 还是直接 ship 源 .vue?我推 (b) ship 源 .vue,简单且业界通行
2. **`tags.md` 标签云组件:`tags` 字段来源** —— 只用 frontmatter `tags:`,还是要同时支持正文 `#tag` 语法?后者要扩展 markdown-it 多一条 inline 规则,工作量额外 ~80 行。我推 v0.2 先只 frontmatter,正文 `#tag` 放 v0.3
3. **`VaultGraph` 是否需要"按 tag 染色"功能** —— 多花 ~50 行,UX 增益中等。推荐 v0.2 不做,v0.3 加

---

## 13. 接下来

你回复:
- 同意方案(对 §12 三个问题给答案)→ 我进实施
- 想改架构 → 告诉我具体改哪
- 想增减视图/功能 → 告诉我
