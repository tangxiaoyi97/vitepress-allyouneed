---
title: Views overview
tags: [views]
---

# Views overview

The build generates the <code>graph</code>, <code>stats</code>, and
<code>tags</code> perspective pages and injects their links into navigation.

The data file uses a non-default name and a custom VitePress `publicDir`.
