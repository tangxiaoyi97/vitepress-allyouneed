# e2e-vault

Small, intentionally finite VitePress fixture. It exercises a real SSR build
against the package's self-reference; it is not a user-facing documentation
site. Run from the package root with `npm run test:e2e`.
